{
  "name": "vardanstallning",
  "version": "2.2.0",
  "private": true,
  "description": "Sök och bevaka jobb inom vård och omsorg i Sverige.",
  "scripts": {"check": "node --check api/_jobs-core.js && node --check api/_db.js && node --check api/jobs.js && node --check api/feed.js && node --check api/alerts.js && node --check app.js"},
  "dependencies": {"@neondatabase/serverless":"^1.0.2"},
  "engines": {"node": ">=20"}
}
